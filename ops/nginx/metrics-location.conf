# Prometheus scrape endpoint — bypass JWT, restrict to loopback/internal.
# Mount внутри server { } блока vhost'а svoefoto.ru.
location = /metrics {
    allow 127.0.0.1;
    allow 10.0.0.0/8;
    deny all;
    proxy_pass http://127.0.0.1:3001/metrics;
    proxy_set_header Host $host;
}
